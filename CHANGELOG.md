# Changelog

## 未发布

### 新增功能

- 飞书 / Lark 链接增加独立文档读取步骤：支持 Chrome 桌面正文导入和官方 Lark CLI 读取，保存原文、章节覆盖及未读附件；材料就绪后单独开始或恢复讨论。
- 标准 Plan 增加内联 SVG 逻辑蓝图要求及静态样式示例，兼顾窄屏、离线查看和打印。
- 任务顶部展示 Worktree 名称、状态、分支和路径，并支持复制路径。
- 人工验收返修部分完成且已有验收清单时，可明确接受当前断点并返回人工验收；保留自动自检未完成的记录和人工门禁。

### 优化与修复

- 文档读取失败可单独重试，保留已有材料与讨论会话；未读附件只提示，缺失正文章节仍阻断讨论与规划。
- Lark CLI 使用服务进程的用户授权读取，Codex 只核验本地材料；分别报告钥匙串、Codex 认证、插件连接和文档权限错误，页面显示后续讨论使用的后台连接。
- Ask 改为默认 120 秒无进度超时、600 秒绝对上限，支持分别配置；禁用子代理、收窄检查范围，并在失败后解除旧会话绑定。
- 保留 Codex JSON 失败事件及 stderr 的具体原因；即使退出码为零，`turn.failed` 也不会被当作成功。
- 非当前阶段只允许回看；返修后页面跟随实际阶段，提交人工验收前先读取最新任务状态。

## 2026-08-31

### 优化

- 提高并发上限：单项目默认最多 8 个后台任务，所有项目合计默认最多 16 个；仍可通过启动环境变量下调。
- 执行与自检真正完成后自动进入人工验收，并在任务收尾和服务重载时校正遗漏的阶段状态。

## 2026-08-19 — 2026-08-25

### 新增功能

- 支持粘贴已有需求并接入工作流。
- 新增任务状态变化通知，支持页面通知和 macOS 通知。
- 新增当前任务一键 Stage、提交文件选择和人工提交确认。
- 新增猫咪品牌图标、网站 favicon 和多尺寸图标资源。

### 优化与修复

- 优化工作流阶段流转、二次确认和验收恢复。
- 优化左侧阶段导航及悬浮弹窗的点击体验。
- 优化任务级 Git 操作，避免 Stage 到不属于当前任务的改动。
- 修复旧任务文件清单和已有暂存内容被误认为当前任务可 Stage 范围的问题。
- 修复知识内容读取超时导致工作流中断的问题。
